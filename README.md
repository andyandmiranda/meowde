# Meowde v4.10 Deploy Package

Static HTML prototype.

## Deploy
1. Upload `index.html`, `vercel.json`, and this README to a GitHub repository.
2. Import the repository in Vercel.
3. Open the deployed URL in a normal browser.

## Note
Pyodide loads from CDN when a Python write task runs, so internet access is required.
